
import React from "react";

const Footer: React.FC = () => {
  return (
    <footer className="text-center py-12">
      <p className="font-semibold">
        "Your star is still shining. Make every moment count before it fades."
      </p>
    </footer>
  );
};

export default Footer;
