import React from "react";
import "@/app/styles/loading.css"; // Import the CSS file for styles

const LoadingCircle: React.FC = () => {
  return <div className="loading-circle" />;
};

export default LoadingCircle;
